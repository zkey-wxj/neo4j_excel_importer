# Privacy Policy

This plugin processes Excel files provided by the user and writes graph data to a user-configured Neo4j instance.

- **No data is sent to third parties.** All processing happens locally within the Dify runtime.
- **Neo4j credentials** are stored as encrypted provider credentials within the Dify platform.
- **Excel files** are processed in memory and not persisted by the plugin.
